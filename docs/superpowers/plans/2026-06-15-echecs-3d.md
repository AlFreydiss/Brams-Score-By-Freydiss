# Échecs vrai 3D (GLTF réaliste) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le faux-3D CSS de l'échecs par un vrai 3D three.js/r3f (plateau orbitable, pièces GLTF réalistes), branché sur la logique de jeu existante sans la toucher.

**Architecture:** Nouveau `Plateau3D.jsx` (r3f) avec la MÊME interface que `Plateau.jsx`, drop-in dans les modes via le toggle 🧊 de `EchecsPage`. La logique de coup (sélection/promotion) est extraite dans un hook partagé `useInteractionEchecs` consommé par les deux plateaux. Les coordonnées et le mapping FEN→pièces sont des modules purs testés. Le modèle GLTF optimisé est servi en same-origin depuis `public/models/`.

**Tech Stack:** React 18, @react-three/fiber@8, @react-three/drei@9, three@0.170, chess.js (via usePartie), node:test.

**Réfs:** spec `docs/superpowers/specs/2026-06-15-echecs-3d-design.md`. Stack r3f déjà installée (package.json). Le site sert `public/` en same-origin (Vite/Vercel).

> **Déviation assumée vs spec** : on héberge le GLB optimisé dans `public/models/echecs3d.glb` (servi `/models/echecs3d.glb`, same-origin) au lieu de R2 → supprime le risque CORS et la dépendance R2.

---

## File Structure

- **Create** `src/features/echecs/lib/coords3d.js` — pur : `squareVers3D(square, orientation)`, `piecesDepuisFen(fen)`.
- **Create** `src/features/echecs/lib/coords3d.test.js` — tests node:test.
- **Create** `src/features/echecs/hooks/useInteractionEchecs.js` — état+logique sélection/coup/promotion (extrait de Plateau).
- **Create** `src/features/echecs/hooks/useInteractionEchecs.test.js` — tests.
- **Create** `src/features/echecs/components/Plateau3D.jsx` — scène r3f.
- **Modify** `src/features/echecs/components/Plateau.jsx` — consomme le hook ; **suppression du chemin CSS-3D** (redevient 2D pur).
- **Modify** `src/features/echecs/EchecsPage.jsx` — rend `<Plateau3D>` quand `troisD`.
- **Modify** `src/features/echecs/constants.js` — `MODELE_3D_URL`, constantes scène 3D ; retrait des constantes CSS-3D devenues inutiles (`TILT_3D_DEG` etc. SI plus référencées).
- **Asset** `public/models/echecs3d.glb` — GLB optimisé (~3-5 Mo).

---

## Task 1: Asset — optimiser le GLTF et le poser en public/

**Files:**
- Create: `public/models/echecs3d.glb`
- Create: `public/models/CREDIT.txt`
- Modify: `src/features/echecs/constants.js`

- [ ] **Step 1: Télécharger la source (43 Mo, CC-BY)**

```bash
mkdir -p /tmp/chess3d && curl -L "https://raw.githubusercontent.com/KhronosGroup/glTF-Sample-Assets/main/Models/ABeautifulGame/glTF-Binary/ABeautifulGame.glb" -o /tmp/chess3d/src.glb
ls -la /tmp/chess3d/src.glb   # ~43 Mo
```

- [ ] **Step 2: Optimiser (Draco + textures WebP réduites) → cible <6 Mo**

```bash
cd /c/Users/Feydi/Desktop/brams-web-clone
mkdir -p public/models
npx --yes @gltf-transform/cli optimize /tmp/chess3d/src.glb public/models/echecs3d.glb --texture-compress webp --texture-size 1024 --compress draco
ls -la public/models/echecs3d.glb
```
Expected: un `.glb` nettement plus petit (~2-5 Mo). Si >6 Mo, relancer avec `--texture-size 512`.

- [ ] **Step 3: Inspecter le graphe pour récupérer les noms de nœuds des pièces**

```bash
npx --yes @gltf-transform/cli inspect public/models/echecs3d.glb | grep -iE "king|queen|rook|bishop|knight|pawn|white|black" | head -40
```
Noter le nom EXACT d'un nœud/mesh par type et couleur (ex. `Object_KingWhite`…). Reporter ce mapping dans `constants.js` (Step 5). Si les pièces ne sont pas individualisées, plan B : `npx @gltf-transform/cli` pour extraire/séparer — mais ce modèle expose bien 32 nœuds nommés.

- [ ] **Step 4: Crédit CC-BY**

Create `public/models/CREDIT.txt`:
```
Modèle 3D "A Beautiful Game" — Khronos Group, glTF-Sample-Assets.
Licence CC-BY 4.0. https://github.com/KhronosGroup/glTF-Sample-Assets
Optimisé (Draco + WebP) pour Brams Community.
```

- [ ] **Step 5: Constantes**

Dans `src/features/echecs/constants.js`, ajouter en fin de fichier :
```js
// ── Échiquier 3D (vrai 3D r3f) ──────────────────────────────────────────────
export const MODELE_3D_URL = '/models/echecs3d.glb'
// Noms de nœuds du GLB par type+couleur (résolus via `gltf-transform inspect`,
// Task 1 Step 3). 'w'/'b' = couleur, type chess.js p/n/b/r/q/k.
export const NOEUDS_PIECES_3D = {
  // À RENSEIGNER avec les noms réels relevés au Step 3, ex :
  // w: { p: 'PawnWhite', n: 'KnightWhite', b: 'BishopWhite', r: 'RookWhite', q: 'QueenWhite', k: 'KingWhite' },
  // b: { p: 'PawnBlack', ... },
}
export const CREDIT_3D = 'Modèle : A Beautiful Game (Khronos) — CC-BY 4.0'
```

- [ ] **Step 6: Commit**

```bash
git add public/models/echecs3d.glb public/models/CREDIT.txt src/features/echecs/constants.js
git commit -m "feat(echecs): asset 3D optimisé (A Beautiful Game CC-BY) en public/models + constantes"
```

---

## Task 2: coords3d.js — mapping cases ↔ 3D et FEN → pièces (pur, TDD)

**Files:**
- Create: `src/features/echecs/lib/coords3d.js`
- Test: `src/features/echecs/lib/coords3d.test.js`

- [ ] **Step 1: Test qui échoue**

Create `src/features/echecs/lib/coords3d.test.js`:
```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { squareVers3D, piecesDepuisFen } from './coords3d.js'

test('squareVers3D centre le plateau (white)', () => {
  assert.deepEqual(squareVers3D('e4', 'white'), [0.5, 0, 0.5])
  assert.deepEqual(squareVers3D('a1', 'white'), [-3.5, 0, 3.5])
  assert.deepEqual(squareVers3D('h8', 'white'), [3.5, 0, -3.5])
})

test('squareVers3D inverse pour black', () => {
  assert.deepEqual(squareVers3D('a1', 'black'), [3.5, 0, -3.5])
})

test('piecesDepuisFen position de départ = 32 pièces', () => {
  const start = 'rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1'
  const ps = piecesDepuisFen(start)
  assert.equal(ps.length, 32)
  assert.ok(ps.some(p => p.square === 'e1' && p.type === 'k' && p.couleur === 'w'))
  assert.ok(ps.some(p => p.square === 'e8' && p.type === 'k' && p.couleur === 'b'))
  assert.ok(ps.some(p => p.square === 'a2' && p.type === 'p' && p.couleur === 'w'))
})
```

- [ ] **Step 2: Lancer → échoue**

Run: `node --test src/features/echecs/lib/coords3d.test.js`
Expected: FAIL (module/exports absents).

- [ ] **Step 3: Implémenter**

Create `src/features/echecs/lib/coords3d.js`:
```js
// Mapping cases d'échecs ↔ coordonnées 3D, et FEN → liste de pièces.
// Repère : plateau centré à l'origine, case de 1 unité. Fichier a..h → x,
// rangée 1..8 → z. 'white' face au joueur (z+ devant). y=0 (posées sur le plateau).
const FICHIERS = 'abcdefgh'

export function squareVers3D(square, orientation = 'white') {
  const f = FICHIERS.indexOf(square[0])        // 0..7
  const r = parseInt(square[1], 10) - 1         // 0..7
  let x = f - 3.5
  let z = 3.5 - r
  if (orientation === 'black') { x = -x; z = -z }
  return [x, 0, z]
}

// FEN champ 1 = placement (rangées 8→1, séparées par '/'). Majuscule = blanc.
export function piecesDepuisFen(fen) {
  const placement = String(fen).split(' ')[0]
  const rangs = placement.split('/')
  const out = []
  for (let i = 0; i < 8; i++) {
    const r = 8 - i
    let f = 0
    for (const ch of rangs[i] || '') {
      if (/\d/.test(ch)) { f += parseInt(ch, 10); continue }
      out.push({
        square: FICHIERS[f] + r,
        type: ch.toLowerCase(),
        couleur: ch === ch.toUpperCase() ? 'w' : 'b',
      })
      f++
    }
  }
  return out
}
```

- [ ] **Step 4: Lancer → passe**

Run: `node --test src/features/echecs/lib/coords3d.test.js`
Expected: `pass 3`.

- [ ] **Step 5: Commit**

```bash
git add src/features/echecs/lib/coords3d.js src/features/echecs/lib/coords3d.test.js
git commit -m "feat(echecs): coords3d — mapping cases↔3D + FEN→pièces (testé)"
```

---

## Task 3: useInteractionEchecs — logique de coup partagée (extraction, TDD)

**Files:**
- Create: `src/features/echecs/hooks/useInteractionEchecs.js`
- Test: `src/features/echecs/hooks/useInteractionEchecs.test.js`
- Modify: `src/features/echecs/components/Plateau.jsx`

- [ ] **Step 1: Test de la logique pure de décision**

Le hook s'appuie sur React, mais sa LOGIQUE de décision (que faire au clic d'une case) est pure et testable via une fonction exportée `deciderClic`. Create `src/features/echecs/hooks/useInteractionEchecs.test.js`:
```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { deciderClic } from './useInteractionEchecs.js'

// partie factice minimale
const partieFactice = (trait, legaux) => ({
  trait,
  coupsLegaux: (sq) => legaux[sq] || [],
})

test('clic sur sa pièce → sélection', () => {
  const p = partieFactice('w', { e2: [{ from: 'e2', to: 'e4' }] })
  const r = deciderClic({ square: 'e2', pieceCouleur: 'w', selection: null, partie: p, peutJouer: () => true })
  assert.deepEqual(r, { type: 'select', square: 'e2' })
})

test('clic sur case légale depuis sélection → coup', () => {
  const p = partieFactice('w', { e2: [{ from: 'e2', to: 'e4' }] })
  const r = deciderClic({ square: 'e4', pieceCouleur: null, selection: 'e2', partie: p, peutJouer: () => true })
  assert.deepEqual(r, { type: 'move', from: 'e2', to: 'e4' })
})

test('clic ailleurs → désélection', () => {
  const p = partieFactice('w', { e2: [{ from: 'e2', to: 'e4' }] })
  const r = deciderClic({ square: 'h7', pieceCouleur: 'b', selection: 'e2', partie: p, peutJouer: () => true })
  assert.deepEqual(r, { type: 'deselect' })
})

test('clic sur pièce adverse sans sélection → rien', () => {
  const p = partieFactice('w', {})
  const r = deciderClic({ square: 'e7', pieceCouleur: 'b', selection: null, partie: p, peutJouer: () => true })
  assert.deepEqual(r, { type: 'deselect' })
})
```

- [ ] **Step 2: Lancer → échoue**

Run: `node --test src/features/echecs/hooks/useInteractionEchecs.test.js`
Expected: FAIL.

- [ ] **Step 3: Implémenter le hook + la fonction pure**

Create `src/features/echecs/hooks/useInteractionEchecs.js`:
```js
import { useState, useMemo, useCallback } from 'react'

// Décision pure : que faire quand on clique `square` ? Renvoie une action.
export function deciderClic({ square, pieceCouleur, selection, partie, peutJouer }) {
  if (selection) {
    const legaux = partie.coupsLegaux(selection)
    if (legaux.some(m => m.to === square)) return { type: 'move', from: selection, to: square }
  }
  if (pieceCouleur && pieceCouleur === partie.trait && peutJouer?.(pieceCouleur)) {
    return { type: 'select', square }
  }
  return { type: 'deselect' }
}

// Hook partagé par Plateau (2D) et Plateau3D : sélection, coups légaux affichés,
// promotion. `partie` = retour usePartie. `pieceSur(square)` → 'w'|'b'|null.
export function useInteractionEchecs(partie, { peutJouer, onCoup, interactif = true } = {}) {
  const [selection, setSelection] = useState(null)
  const [promo, setPromo] = useState(null) // { from, to }

  const coupsLegauxSel = useMemo(
    () => (selection ? partie.coupsLegaux(selection) : []),
    [selection, partie.fen]                       // eslint-disable-line react-hooks/exhaustive-deps
  )

  const tenter = useCallback((from, to) => {
    const candidats = partie.coupsLegaux(from).filter(m => m.to === to)
    if (!candidats.length) return
    if (candidats.some(m => m.promotion)) { setPromo({ from, to }); setSelection(null); return }
    const mv = partie.jouer({ from, to })
    if (mv) { setSelection(null); onCoup?.(mv) }
  }, [partie, onCoup])

  const onCaseClic = useCallback((square, pieceCouleur) => {
    if (!interactif) return
    const a = deciderClic({ square, pieceCouleur, selection, partie, peutJouer })
    if (a.type === 'move') tenter(a.from, a.to)
    else if (a.type === 'select') setSelection(a.square)
    else setSelection(null)
  }, [interactif, selection, partie, peutJouer, tenter])

  const choisirPromotion = useCallback((p) => {
    if (!promo) return
    const mv = partie.jouer({ from: promo.from, to: promo.to, promotion: p })
    setPromo(null)
    if (mv) onCoup?.(mv)
  }, [promo, partie, onCoup])

  return { selection, coupsLegauxSel, promo, onCaseClic, choisirPromotion, annulerPromo: () => setPromo(null) }
}
```

- [ ] **Step 4: Lancer → passe**

Run: `node --test src/features/echecs/hooks/useInteractionEchecs.test.js`
Expected: `pass 4`.

- [ ] **Step 5: Refactor Plateau.jsx pour consommer le hook + retirer le CSS-3D**

Dans `src/features/echecs/components/Plateau.jsx` : remplacer l'état/logique internes (`caseSelection`, `promo`, `tenterCoup`, `choisirPromotion`, handlers) par `useInteractionEchecs`. **Supprimer entièrement le bloc CSS-3D** (`CSS_3D`, branche `troisD`, le return 3D) → `Plateau` redevient 2D pur (props `troisD` ignorée/retirée). Garder drag&drop + clic via les handlers react-chessboard, mais router la sélection/coup via `onCaseClic`. Les `customSquareStyles` utilisent `selection`/`coupsLegauxSel` du hook. Vérifier que le mode 2D marche toujours (build + clic-clic). Conserver `SelecteurPromotion` piloté par `promo`/`choisirPromotion`.

- [ ] **Step 6: Build + tests**

Run: `npm run build` → `✓ built`
Run: `node --test src/features/echecs/hooks/useInteractionEchecs.test.js src/features/echecs/lib/coords3d.test.js` → `pass 7`.

- [ ] **Step 7: Commit**

```bash
git add src/features/echecs/hooks/useInteractionEchecs.js src/features/echecs/hooks/useInteractionEchecs.test.js src/features/echecs/components/Plateau.jsx
git commit -m "refactor(echecs): logique de coup extraite (useInteractionEchecs), Plateau 2D pur"
```

---

## Task 4: Plateau3D.jsx — scène r3f (board + pièces GLTF + interaction)

**Files:**
- Create: `src/features/echecs/components/Plateau3D.jsx`

- [ ] **Step 1: Implémenter la scène**

Create `src/features/echecs/components/Plateau3D.jsx`:
```jsx
// Échiquier vrai 3D (react-three-fiber). MÊME interface que Plateau : drop-in.
// Logique de coup déléguée à useInteractionEchecs ; pièces clonées du GLB.
import { Suspense, useMemo, useRef } from 'react'
import { Canvas, useFrame } from '@react-three/fiber'
import { OrbitControls, useGLTF, Environment, ContactShadows, Html } from '@react-three/drei'
import * as THREE from 'three'
import { THEME, MODELE_3D_URL, NOEUDS_PIECES_3D, CREDIT_3D } from '../constants.js'
import { squareVers3D, piecesDepuisFen } from '../lib/coords3d.js'
import { useInteractionEchecs } from '../hooks/useInteractionEchecs.js'
import SelecteurPromotion from './SelecteurPromotion.jsx'

const FICHIERS = 'abcdefgh'
useGLTF.preload(MODELE_3D_URL)

// Récupère un mesh cloné par type+couleur depuis le GLB (noms = NOEUDS_PIECES_3D).
function useMeshesPieces() {
  const { nodes } = useGLTF(MODELE_3D_URL)
  return useMemo(() => {
    const map = {}
    for (const couleur of ['w', 'b']) {
      map[couleur] = {}
      for (const type of ['p', 'n', 'b', 'r', 'q', 'k']) {
        const nom = NOEUDS_PIECES_3D?.[couleur]?.[type]
        const src = nom && nodes[nom]
        map[couleur][type] = src ? src.geometry : null
        map[couleur][`mat_${type}`] = src ? src.material : null
      }
    }
    return map
  }, [nodes])
}

// Une pièce animée : lerp vers sa position cible (arc pour le cavalier).
function Piece3D({ geometry, material, cible, type }) {
  const ref = useRef()
  useFrame((_, dt) => {
    const o = ref.current; if (!o) return
    const k = Math.min(1, dt * 9)
    o.position.x += (cible[0] - o.position.x) * k
    o.position.z += (cible[2] - o.position.z) * k
    // petit saut pour le cavalier en cours de déplacement
    const d = Math.hypot(cible[0] - o.position.x, cible[2] - o.position.z)
    o.position.y = type === 'n' ? Math.min(0.6, d) : 0
  })
  if (!geometry) return null
  return <mesh ref={ref} geometry={geometry} material={material} position={cible} castShadow receiveShadow scale={1} />
}

function Echiquier({ orientation, surbrillances, onCaseClic, pieceSur }) {
  const cases = []
  for (let f = 0; f < 8; f++) for (let r = 0; r < 8; r++) {
    const square = FICHIERS[f] + (r + 1)
    const [x, , z] = squareVers3D(square, orientation)
    const claire = (f + r) % 2 === 1
    const sb = surbrillances[square]
    cases.push(
      <mesh key={square} position={[x, -0.05, z]} receiveShadow
        onClick={(e) => { e.stopPropagation(); onCaseClic(square, pieceSur(square)) }}>
        <boxGeometry args={[1, 0.1, 1]} />
        <meshStandardMaterial color={sb?.color || (claire ? THEME.caseClaire : THEME.caseFoncee)} />
        {sb?.legal && (
          <mesh position={[0, 0.06, 0]}><cylinderGeometry args={[0.16, 0.16, 0.02, 24]} />
            <meshStandardMaterial color={THEME.gold} emissive={THEME.gold} emissiveIntensity={0.6} /></mesh>
        )}
      </mesh>
    )
  }
  return <group>{cases}</group>
}

function Scene({ partie, orientation, inter, pieceSur }) {
  const meshes = useMeshesPieces()
  const pieces = useMemo(() => piecesDepuisFen(partie.fen), [partie.fen])

  const surbrillances = useMemo(() => {
    const s = {}
    if (partie.dernierCoup) { s[partie.dernierCoup.from] = { color: THEME.dernierCoup }; s[partie.dernierCoup.to] = { color: THEME.dernierCoup } }
    if (inter.selection) s[inter.selection] = { color: THEME.gold }
    for (const m of inter.coupsLegauxSel) s[m.to] = { ...(s[m.to] || {}), legal: true }
    if (partie.caseRoiEnEchec) s[partie.caseRoiEnEchec] = { color: THEME.echecRoi }
    return s
  }, [partie.dernierCoup, partie.caseRoiEnEchec, inter.selection, inter.coupsLegauxSel])

  return (
    <>
      <Echiquier orientation={orientation} surbrillances={surbrillances} onCaseClic={inter.onCaseClic} pieceSur={pieceSur} />
      {pieces.map((p) => (
        <Piece3D key={p.square}
          geometry={meshes[p.couleur]?.[p.type]}
          material={meshes[p.couleur]?.[`mat_${p.type}`]}
          cible={squareVers3D(p.square, orientation)} type={p.type} />
      ))}
    </>
  )
}

export default function Plateau3D({ partie, orientation = 'white', peutJouer, onCoup, interactif = true }) {
  const inter = useInteractionEchecs(partie, { peutJouer, onCoup, interactif })
  // pieceSur(square) → 'w' | 'b' | null (depuis le FEN)
  const pieceSur = useMemo(() => {
    const m = {}; for (const p of piecesDepuisFen(partie.fen)) m[p.square] = p.couleur
    return (sq) => m[sq] || null
  }, [partie.fen])

  return (
    <div data-testid="plateau3d-wrap" style={{ position: 'relative', width: '100%', height: 'min(72vh, 560px)' }}>
      <Canvas shadows dpr={[1, 1.6]} camera={{ position: orientation === 'black' ? [0, 7, -7.5] : [0, 7, 7.5], fov: 42 }}>
        <color attach="background" args={['#0b0a0e']} />
        <ambientLight intensity={0.5} />
        <directionalLight position={[6, 12, 6]} intensity={1.3} castShadow shadow-mapSize={[1024, 1024]} />
        <Suspense fallback={<Html center style={{ color: '#cbb26b', font: '700 14px sans-serif' }}>Chargement de l'échiquier 3D…</Html>}>
          <Scene partie={partie} orientation={orientation} inter={inter} pieceSur={pieceSur} />
          <Environment preset="apartment" />
        </Suspense>
        <ContactShadows position={[0, -0.1, 0]} opacity={0.5} scale={12} blur={2.2} far={4} />
        <OrbitControls enablePan={false} minDistance={6} maxDistance={16} maxPolarAngle={Math.PI / 2.15} />
      </Canvas>
      <div style={{ position: 'absolute', bottom: 6, right: 10, fontSize: 9, color: 'rgba(255,255,255,0.32)', pointerEvents: 'none' }}>{CREDIT_3D}</div>
      {inter.promo && (
        <SelecteurPromotion couleur={partie.trait} onChoisir={inter.choisirPromotion} onAnnuler={inter.annulerPromo} />
      )}
    </div>
  )
}
```

> Note implémenteur : si `NOEUDS_PIECES_3D` est vide/incorrect, les pièces ne s'affichent pas → revenir Task 1 Step 3 relever les vrais noms (`useGLTF` expose `nodes` indexé par nom). Logger `console.log(Object.keys(nodes))` une fois pour les obtenir si l'inspect CLI est ambigu.

- [ ] **Step 2: Build**

Run: `npm run build` → `✓ built` (pas d'erreur d'import r3f/three).

- [ ] **Step 3: Commit**

```bash
git add src/features/echecs/components/Plateau3D.jsx
git commit -m "feat(echecs): Plateau3D — scène r3f (board + pièces GLTF + interaction + orbit)"
```

---

## Task 5: Câbler EchecsPage sur Plateau3D

**Files:**
- Modify: `src/features/echecs/EchecsPage.jsx`
- Modify: `src/features/echecs/modes/SoloVsIA.jsx`, `DeuxJoueursLocal.jsx`, `MultiOnline.jsx`

- [ ] **Step 1: Rendre Plateau3D quand troisD**

Les modes reçoivent déjà `troisD` et rendent `<Plateau ... troisD={troisD} />`. Remplacer, dans chaque mode (`SoloVsIA` PartieSolo, `DeuxJoueursLocal`, `MultiOnline`), le rendu par un choix de composant :
```jsx
import Plateau from '../components/Plateau.jsx'
import Plateau3D from '../components/Plateau3D.jsx'
// ...
const PlateauComp = troisD ? Plateau3D : Plateau
return (
  <PlateauComp
    partie={partie}
    orientation={orientation}
    peutJouer={peutJouer}
    onCoup={onCoup}
    interactif={interactif}
    /* taille: ignorée par Plateau3D (plein conteneur) ; gardée pour le 2D */
    taille={taillePlateau}
  />
)
```
(Adapter aux noms de props locaux de chaque mode — l'interface `partie/orientation/peutJouer/onCoup/interactif` est identique.) `Plateau3D` ignore `taille`/`troisD`. Garder le bouton toggle 🧊 de `EchecsPage` tel quel (il pilote `troisD`).

- [ ] **Step 2: Build**

Run: `npm run build` → `✓ built`.

- [ ] **Step 3: Commit**

```bash
git add src/features/echecs/EchecsPage.jsx src/features/echecs/modes/SoloVsIA.jsx src/features/echecs/modes/DeuxJoueursLocal.jsx src/features/echecs/modes/MultiOnline.jsx
git commit -m "feat(echecs): le toggle 3D rend le vrai Plateau3D r3f dans tous les modes"
```

---

## Task 6: Fallback WebGL + acceptation + deploy

**Files:**
- Modify: `src/features/echecs/components/Plateau3D.jsx`

- [ ] **Step 1: Fallback si WebGL indisponible**

En tête de `Plateau3D`, avant le `<Canvas>`, détecter l'absence de WebGL et retomber sur le 2D :
```jsx
import Plateau from './Plateau.jsx'
// dans le composant, tout en haut :
const webgl = useMemo(() => {
  try { const c = document.createElement('canvas'); return !!(window.WebGLRenderingContext && (c.getContext('webgl') || c.getContext('experimental-webgl'))) } catch { return false }
}, [])
if (!webgl) return <Plateau partie={partie} orientation={orientation} peutJouer={peutJouer} onCoup={onCoup} interactif={interactif} taille={480} />
```

- [ ] **Step 2: Acceptation (webapp-testing)**

Invoquer la skill `webapp-testing` : `/echecs` → "2 joueurs (local)" → activer 🧊 3D → vérifier : chargement du modèle (pièces visibles), clic-clic joue un coup (ex. e2→e4), surbrillances (sélection or + disques coups légaux), pièce animée, orbit caméra (drag souris), promotion (pousser un pion à dame), échec (halo). Capturer un screenshot. Re-basculer 2D → toujours fonctionnel. Si le rendu casse, vérifier `NOEUDS_PIECES_3D` (Task 1 Step 3).

- [ ] **Step 3: Build + tests complets**

Run: `npm run build` → `✓ built`
Run: `node --test src/features/echecs/lib/coords3d.test.js src/features/echecs/hooks/useInteractionEchecs.test.js` → `pass 7`.

- [ ] **Step 4: Commit + deploy**

```bash
git add src/features/echecs/components/Plateau3D.jsx
git commit -m "feat(echecs): fallback 2D si WebGL indisponible"
git push
vercel --prod --scope brams-score-by-freydiss-projects --yes
```
Vérifier `/echecs` en prod : toggle 3D charge le vrai échiquier.

---

## Notes d'exécution
- L'asset (Task 1) est le seul point manuel/risqué : bien relever les **noms de nœuds** (Step 3) → ils alimentent `NOEUDS_PIECES_3D`, dont dépend tout l'affichage des pièces. Si le modèle expose mal les pièces, plan B = séparer en 6 GLB via `gltf-transform`.
- Le rendu r3f n'est pas unit-testable → la garde = build vert + acceptation `webapp-testing`. Les modules purs (coords3d, deciderClic) sont en vrai TDD.
- `taillePlateauAuto(troisD)` (constants.js) peut rester ; `Plateau3D` prend tout le conteneur (height responsive).
