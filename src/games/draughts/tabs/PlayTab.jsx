// PlayTab (Dames) — STUB. L'agent construit ici le damier 2D 10×10 (drag+clic,
// rafle max forcée, dames volantes), IA minimax Worker, move list, captures, contrôles.
import { ui, fonts } from '../../../features/games/neutralTheme.js'
export default function PlayTab() {
  return (
    <div style={{ height: '100%', display: 'grid', placeItems: 'center', color: ui.textDim, font: `600 14px ${fonts.body}` }}>
      Damier — en construction
    </div>
  )
}
