// RulesTab (Échecs) — STUB. L'agent construit ici les règles pédagogiques sobres
// (mini-diagrammes 2D du board en lecture seule, sections aérées).
import { ui, fonts } from '../../../features/games/neutralTheme.js'
export default function RulesTab() {
  return (
    <div style={{ height: '100%', display: 'grid', placeItems: 'center', color: ui.textDim, font: `600 14px ${fonts.body}` }}>
      Règles — en construction
    </div>
  )
}
